#ifndef EXIT_H
#define EXIT_H

enum {
    // This is already defined in a macro; it is included here for completeness.
 /* EXIT_SUCCESS = 0 */
    EXIT_FAILED_ALLOC = 1,
	EXIT_BAD_PARAMS   = 2
};

#endif
