#include <stdio.h>

#include "object.h"
#include "plane.h"

// TODO: Write this.
obj_t* init_plane(FILE *in, object_id id) {
	in = NULL;
	id = PLANE;
	return NULL;
}

// TODO: Write this.
void dump_plane(FILE *out, obj_t *obj) {
	out = NULL;
	obj = NULL;
}
